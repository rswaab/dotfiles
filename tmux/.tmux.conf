# Enable mouse support
set -g mouse on

# Sync tmux clipboard with system clipboard (macOS)
set -g set-clipboard on

# Project jump bindings: prefix + I/P → cd + launch claude in current pane
bind-key I send-keys 'cd ~/Dev/projects/ila-app && claude' Enter
bind-key P send-keys 'cd ~/Dev/projects && claude' Enter
